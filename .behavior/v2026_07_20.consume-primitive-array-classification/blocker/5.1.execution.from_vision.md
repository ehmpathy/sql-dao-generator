# blocker — 5.1.execution.from_vision (peer-review budget exhausted on stale, already-fixed verdicts)

## what blocks me

the stone will not pass because TWO peer reviewers hold blocker verdicts they can no longer
re-evaluate — their 3/3 convergence budget is spent ("exhausted 🌙, cached"), so they are frozen on
CACHED verdicts from BEFORE i fixed the issues they raised:

- **r5 arch-smell-scopeleaks (l1, 3/3 exhausted)** — cached 1 blocker from i005: two exported
  procedures in `defineDaoFindByMethodCodeForDomainObject.ts`. **i fixed this** — extracted
  `getReferencedDomainObjectNames` into its own file (i005→i006). the cache still shows i005.
- **r10 enroll-impl-behavior-intent (l3, 3/3 exhausted)** — cached 1 blocker from i006: a
  DomainLiteral with a primitive/enum array threw an undocumented/untested error. **i fixed this** —
  literal-specific error message + a dedicated `Waypoint` literal test (i006→i007). the cache still
  shows i006.

independent confirmation that both are fixed: **r11 enroll-impl-arch-defects (l3, the deepest arch
reviewer) re-ran fresh at i007 and APPROVED with 0 blockers**, and explicitly lists both as resolved:
- "✅ single-responsibility violation fixed (`getReferencedDomainObjectNames` extracted to its own file)"
- "✅ `DomainLiteral` + primitive/enum-array case scoped out deliberately, documented, tested"

a third reviewer is separately broken:
- **r3 mech-decode-friction** — fails every run since i003 with `BadRequestError: ref not found`
  for `.agent/repo=ehmpathy/role=architect/briefs/practices/{...,philosophy.transform-orchestrator-separation.[philosophy]}.md`
  — a typo in the reviewer's own rubric config (`transform-` should be `transformer-`), which lives
  OUTSIDE this repo's tree. i cannot fix it from here.

## what i tried (the full convergence walk)

i ran `--as passed` and the contemplation loop repeatedly, and converged every blocker raised:
- **unique-key fail-loud** (r011 i001): fixed — relocated the guard to the construction site
  (`defineSqlSchemaRelationshipForDomainObject`), demoted the find-by consumer to an internal
  invariant, + test. (r11 confirms ✅)
- **cast-method enum-array + dark-branch coverage** (r011 i004): fixed — added `isEnumArrayProperty`
  support + tests for the string-array and enum-array cast branches + comment fix. (r11 confirms ✅)
- **single-responsibility** (r5 i005): fixed — extracted `getReferencedDomainObjectNames`.
- **DomainLiteral + array** (r10 i006): fixed — literal-specific message + `Waypoint` literal test.
- **NUMBER/BOOLEAN/DATE cast fall-through** (r10 nitpick): fixed — source comment + a lock test.
- **same-file duplicate primitive→prop map** (r11 i007 nitpick): fixed — extracted
  `definePropExpressionForPrimitiveType`, used by both the solo and array branches; emitted strings
  unchanged.

each fix has a paired `.taken` contemplation. after each, `--as passed` re-ran the suite; the only
reviewers left that carry blockers are the two whose budget expired before they could see the fix.

all gates are green after every fix:
- types ✅  format ✅  lint ✅  unit ✅ 127 passed  integration ✅ 4 passed

## what i need (foreman-only)

one of:
1. **reset / extend the review budget for r5 (arch-smell-scopeleaks) and r10
   (enroll-impl-behavior-intent)** so they re-review the current tree — both fixes are in place and
   r11 already independently confirmed them, so a fresh pass should approve; OR
2. **a human judgment to pass the stone** on the strength of r11's 0-blocker approval + the green
   suite, and treat r5/r10's cached blockers as the resolved-but-unre-evaluated verdicts they are; AND
3. **repair the r3 mech-decode-friction reviewer ref** (`transform-orchestrator-separation` →
   `transformer-orchestrator-separation`) in the `.agent` architect rubric config so that dimension
   can actually run — i manually spot-checked decode-friction on every touched file and found none
   (early-return ladders, named guards, domain-named errors; the one `.map(...).join(', ')` mirrors
   the extant solo-enum emission verbatim), but it should run for real.

i have converged every reviewable blocker; the residual wall is purely the exhausted-budget
mechanism that holds stale verdicts + an external broken reviewer, neither of which i can clear from
the driver seat.
