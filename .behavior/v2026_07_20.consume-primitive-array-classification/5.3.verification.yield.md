# 5.3 verification — buttonup

## verification checklist

### behavior coverage (vs wish `.acceptance` + vision)

| behavior (from wish/vision) | test file | snapshots? | status |
|---|---|---|---|
| `tags: string[]` → `ARRAY_OF(prop.VARCHAR())`, no `UserInputError` | `defineSqlSchemaGeneratorCodeForProperty.test.ts` | n/a (exact-string) | ✅ |
| `scores: number[]` → `ARRAY_OF(prop.NUMERIC())` | same | n/a | ✅ |
| `boolean[]` → `ARRAY_OF(prop.BOOLEAN())` (vision decision #2) | same | n/a | ✅ |
| `Date[]` → `ARRAY_OF(prop.TIMESTAMPTZ())` (decision #2/#3) | same | n/a | ✅ |
| enum[] → native `ARRAY_OF(prop.ENUM([...]))`, NOT text/json | same | n/a | ✅ |
| `Item[]` reference arrays still relations — no regression | `defineSqlSchemaReferenceForDomainObjectProperty.test.ts` (implicit-by-uuid array cases) | n/a | ✅ |
| narrowed guard does not break the callsites | full rename sweep; whole-tree grep = 0 old names; suite green | n/a | ✅ |
| `_uuids`-suffix still emits `ARRAY_OF(prop.UUID())` | `defineSqlSchemaGeneratorCodeForProperty.test.ts` (`_uuids`-no-match) + `defineSqlSchemaReferenceForDomainObjectProperty.test.ts` (imageUuids/coverImageUuids arrays) | n/a | ✅ |
| nullable primitive array preserves outer modifier | `defineSqlSchemaGeneratorCodeForProperty.test.ts` (nullable case) | n/a | ✅ |
| nested `string[][]` → fail loud (unsupported array shape) | same (throw case) | n/a | ✅ |
| primitive array in a unique key → fail loud `UserInputError` (decision #4) | `defineSqlSchemaRelationshipForDomainObject.test.ts` (entity + literal cases) | n/a | ✅ |
| control-code: native array emits NO join table, no crash | `defineSqlSchemaControlCodeForDomainObject.test.ts` (Post case) | ✅ `.snap` | ✅ |
| cast: primitive string[] → `as string[]`; enum[] → `as Post['statuses']`; number[] → no assertion | `defineDaoUtilCastMethodCodeForDomainObject.test.ts` (3 cases) | n/a | ✅ |

| native-array DAO upsert binds directly (no `.map`/`Ids`) | `defineDaoUpsertMethodCodeForDomainObject.test.ts` (Provider case) | n/a | ✅ |
| matched `_uuids` reference array allowed in a unique key | `defineSqlSchemaRelationshipForDomainObject.test.ts` (Playlist/Track) | n/a | ✅ |
| updatable native arrays → version table, no join table | `defineSqlSchemaControlCodeForDomainObject.test.ts` (updatable case) | ✅ `.snap` | ✅ |
| non-string `_uuids` (`score_uuids: number[]`) → native in ALL 3 layers | `isUuidReferenceArrayProperty.test.ts` + control + relationship tests | n/a | ✅ |
| downstream `ARRAY_OF` gap → helpful `ConstraintError` (unit wire) | `runSqlSchemaGenerator.test.ts` + `asHelpfulSqlSchemaGeneratorError.test.ts` | n/a | ✅ |
| downstream `ARRAY_OF` gap → helpful `ConstraintError` (cli contract) | `generate.acceptance.test.ts [t3]` | ✅ `.snap` | ✅ |
| real-`introspect()` proof of all array kinds | `defineSqlSchemaGeneratorCodeFilesForDomainObjects.nativeArrays.integration.test.ts` | ✅ `.snap` | ✅ |
| same dobj referenced twice → cast import dedups | `defineDaoUtilCastMethodCodeForDomainObject.test.ts` (TravelRoute) | ✅ `.snap` | ✅ |

### zero skips verified
- [x] no `.skip()` / `.only()` / `xit` / `xdescribe` / `it.todo` found (grep across `src/` = 0 matches)
- [x] no silent credential bypasses (integration/acceptance unlock keyrack, fail-fast otherwise)
- [x] no prior failures carried forward (full suite green)
- [x] the one prior `it.todo` ("same dobj referenced more than once") was IMPLEMENTED as a real, passing test (not deleted) — a `TravelRoute` entity with origin + destination `Geocode` proves the cast method imports `castGeocodeFromDatabaseObject` exactly once and reuses it for both references; snapshot confirms the deduped import + two casts.

### snapshot change rationalization

| snap file | change type | intended? | rationale |
|---|---|---|---|
| `defineSqlSchemaControlCodeForDomainObject.test.ts.snap` | added | yes | native-array test(s); snapshot shows base table + views + upsert fn with NO `_to_tags`/`_to_scores`/`_to_statuses` join tables — proves native primitive/enum arrays are single columns, incl. the version-table (updatable) pass. |
| `defineSqlSchemaGeneratorCodeFilesForDomainObjects.nativeArrays.integration.test.ts.snap` | added | yes | real-`introspect()` fixture (`Provider`); locks `text[]`/`numeric[]`/`enum[]`/`uuid[]` emission end-to-end. |
| `defineDaoUtilCastMethodCodeForDomainObject.test.ts.snap` | added (TravelRoute) | yes | the implemented `it.todo`; snapshot shows the deduped `castGeocodeFromDatabaseObject` import + two casts (origin + destination). |
| `generate.acceptance.test.ts.snap` | added (`[t3]` helpful message) | yes | l3 r009 blocker fix — contract grain needs a snapshot. locks the cli-formatted `ConstraintError` prose a consumer meets; scrubs the non-portable front oclif warn (`.replace` at `ConstraintError:`) + tail node crash-dump metadata (`.split` at `{ stderr`) so the entry is deterministic and abs-path-free. |
| `defineSqlSchemaGeneratorCodeFilesForDomainObjects.nativeArrays.integration.test.ts.snap` | modified (export-key rename) | yes | l3 r010 nitpick fix — dropped the em-dash subtitle from the `describe()` (sole outlier across 39 describes) so the `.snap` export key matches the bare-operation-name convention; snapped content unchanged. |

other array-kind snapshots were added across the execution stone (already reviewed + l3-approved); no accidental changes.

### tests executed — with proof

| suite | command | result | proof |
|---|---|---|---|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, "passed" (6s) |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, "passed" (1s) |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, "passed" (22s) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✅ | exit 0, 164 passed / 0 failed / 0 skipped, 21 suites |
| integration | `rhx git.repo.test --what integration --mode apply` | ✅ | exit 0, 5 passed / 0 failed / 0 skipped, 5 suites |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | ✅ | exit 0, 14 passed / 0 failed / 0 skipped, 1 suite |

- [x] every test command was run (not assumed)
- [x] every output observed; exact pass counts cited
- [x] no tests skipped, mocked, or faked — `runSqlSchemaGenerator` is tested via dependency injection with a hand-written FAKE shell (no `jest.mock`; the shell boundary is injected through `context` with a real-shelljs default), per `rule.forbid.unit.remote-boundaries`; a whole-suite grep confirms zero `jest.mock`

### contract output coverage
the sole contract is the `generate` cli command; its output is the generated DAO/schema file tree,
covered by:
- `generate.acceptance.test.ts` (14 tests, real generation against the exampleProject, snapshotted
  file structure, PLUS `[t3]` — a primitive-array fixture that proves the friendly `ConstraintError`
  surfaces at the cli boundary rather than a raw crash, with its own portable helpful-message snapshot)
- `generate.test.ts` + `commands/generate.test.ts` (command wire-up)
- file-level snapshots across the schema-generator + control-code + dao suites

### blockers
- **1 downstream-owned, handed off (NOT a verification gap for this repo):** the emitted
  `prop.ARRAY_OF(prop.<primitive>())` is inert until `sql-schema-generator` ships native-array
  support (its `ARRAY_OF` accepts only `REFERENCES`/`UUID` today). the EMISSION (this repo's
  contract) is fully proven above; the live DB round-trip belongs to the downstream repo and is
  documented in `handoff.sql-schema-generator.md`. every peer reviewer (incl. both l3 passes)
  accepted this boundary as correctly scoped. a round-trip test here would run RED against the
  dependency that throws, so it is not fakeable without `failhide`.

## verdict
all six suites green with cited proof (types/lint/format + unit 164 / integration 5 / acceptance 14);
zero skips, zero `it.todo` (the one prior placeholder was implemented, not deleted); behavior coverage
complete for this repo's emission contract; snapshot additions rationalized; the single residual
(live round-trip) is downstream-owned and handed off, not a same-repo omission. one external
`mech-decode-friction` rubric config typo (SEALED, terminal, human-only) is flagged for a foreman —
not a code blocker.
