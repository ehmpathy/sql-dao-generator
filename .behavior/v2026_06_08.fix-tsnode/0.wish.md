wish =

looks like theres an incompat in our tsnode version

here's what a peer said

are we able to get rid of ts-node all together? like rhachet did ? e.g., see `ehmpathy/rhachet` .bin/run stuff, i think they just use untyped node directly

---

# blocker: 5.1.execution.phase0_to_phaseN

## what blocks me

sql-dao-generator fails with ts-node error when running `npm run generate:dao:postgres`:

```
TypeError: value.replace is not a function
    at normalizeSlashes (.../ts-node/dist/util.js:62:18)
    at Object.getExtendsConfigPath (.../ts-node/dist/ts-internals.js:24:54)
```

**root cause:** `tsconfig.json` uses array syntax for `extends` (TypeScript 5.0+ feature). sql-dao-generator@0.12.0 uses ts-node@10.9.2 internally, which does not support array extends and crashes.

## what i tried

1. verified domain objects exist and compile (`npm run build` passes)
2. added missing `updatable` to ClaimSourceArtifact
3. ran `npm run generate:dao:postgres` — crashed with ts-node error
4. identified root cause: array `extends` in tsconfig.json incompatible with ts-node 10.x

## what i need

fix the ts-node / tsconfig compatibility. options:

| option | effort | risk |
|--------|--------|------|
| create `tsconfig.dao.json` with single-string extends | low | low |
| upgrade sql-dao-generator (if newer uses tsx) | medium | medium |
| pnpm patch ts-node | medium | low |

recommended: create `tsconfig.dao.json` and set `TS_NODE_PROJECT=tsconfig.dao.json` in the generate npm command.

## verification

after fix:

```sh
npm run generate:dao:postgres
# should generate DAOs to src/access/daos/
```

then i can continue phase 0 and subsequent phases.


