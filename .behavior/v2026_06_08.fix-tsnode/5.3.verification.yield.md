# verification yield

## behavior coverage

| behavior from vision | test file | coverage |
|---------------------|-----------|----------|
| ts-node.register removed from bin/run | `src/contract/commands/generate.acceptance.test.ts` | acceptance test verifies CLI works without ts-node |
| ts-node moved to devDependencies only | `package.json` diff | verified via grep: line 137 in devDependencies |
| oclif commands load from ./dist/ | `src/contract/commands/generate.acceptance.test.ts` | acceptance test runs built binary |
| CLI works after build without ts-node | `src/contract/commands/generate.acceptance.test.ts` | 5 acceptance tests pass |

## acceptance test coverage

| test | description | status |
|------|-------------|--------|
| [t0] bin/run --help | verifies CLI shows help output | passed |
| [t0] no ts-node required | verifies no "Cannot find module" errors | passed |
| [t1] bin/run generate | verifies generate command completes with valid config | passed |
| [t1] no ts-node failure | verifies generate does not fail due to absent ts-node | passed |
| [t2] clean NODE_PATH | verifies CLI runs in clean node environment | passed |

## tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types --mode apply` | passed | exit 0, 2s |
| format | `rhx git.repo.test --what format --mode apply` | passed | exit 0, 2s |
| lint | `rhx git.repo.test --what lint --mode apply` | passed | exit 0, 6s |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | passed | 144 tests passed, 0 failed, 33s |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | passed | 5 tests passed, 0 failed, 20s |
| integration | `rhx git.repo.test --what integration --mode apply --thorough` | infra failure | database "superimportantdb" does not exist |

## integration test analysis

**24 tests failed** — all due to absent test database:

```
error: database "superimportantdb" does not exist
```

**root cause:** local test environment lacks PostgreSQL database. CI provisions via `npm run provision:integration-test-db --if-present` (line 169-170 of `.github/workflows/.test.yml`).

**unrelated to ts-node removal:**
1. the ts-node removal affects runtime behavior, not database connectivity
2. the same tests would fail on main without local database
3. CI will provision the database and run tests

**tests that passed locally (14/38):**
- all config/metadata extraction tests
- all code definition/generation tests
- these do not require database connection

## zero skips verified

```bash
grep -r "\.skip\|\.only" src/**/*.test.ts
```
result: no matches in test files

- [x] no .skip() or .only() found
- [x] no silent credential bypasses
- [x] no prior failures carried forward

## snapshot changes

no `.snap` files were modified by this change.

## contract output snapshot exhaustiveness

| contract | test file | snapshots |
|----------|-----------|-----------|
| generate command | `src/contract/commands/generate.acceptance.test.ts` | output verified via assertions (no stdout snapshots in this repo pattern) |

**note:** this repo does not use stdout snapshots for CLI output. acceptance tests verify behavior via assertions.

## blockers

**integration tests require database provision** — see handoff:
- `.behavior/v2026_06_08.fix-tsnode/5.3.verification.handoff.v1.to_foreman.md`

the integration tests require database provision which happens in CI. the ts-node removal does not affect database connectivity. foreman approval needed to proceed.

## files changed

| file | change |
|------|--------|
| `bin/run` | removed `ts-node.register()` call |
| `package.json` | ts-node from deps to devDeps, @types/lodash.omit added, rhachet packages removed |
| `.depcheckrc.yml` | rhachet packages added to ignore list |
| `tsconfig.build.json` | acceptance tests excluded from build |
| `src/contract/commands/generate.acceptance.test.ts` | new acceptance test (5 tests) |
