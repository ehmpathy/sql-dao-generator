# verification handoff: integration tests require database provision

## blocker

integration tests require PostgreSQL database that is not provisioned locally.

## what i tried

1. **ran integration tests locally**
   - command: `rhx git.repo.test --what integration --mode apply --thorough`
   - result: 24 tests failed with `error: database "superimportantdb" does not exist`

2. **attempted to provision database**
   - command: `npm run provision:integration-test-db`
   - result: blocked by permission constraints (requires Docker access)

3. **checked for alternative script**
   - command: `npm run start:testdb`
   - result: script does not exist in this repo (the actual script is `provision:integration-test-db`)

## why this is foreman-only

the database provision requires:
- Docker daemon access
- port 7821 availability
- docker-compose execution

these are infrastructure-level permissions that only the foreman can grant.

## evidence that tests are not broken

1. **14 tests passed locally** - tests that do not require database connection work correctly
2. **CI provisions the database** - `.github/workflows/.test.yml` lines 169-170 run `npm run provision:integration-test-db --if-present`
3. **unrelated to ts-node removal** - the database requirement is unrelated to the code changes in this PR

## tests that pass locally

| suite | result | count |
|-------|--------|-------|
| types | passed | - |
| format | passed | - |
| lint | passed | - |
| unit | passed | 144 |
| acceptance | passed | 5 |

## request

please either:
1. provision the integration test database locally: `npm run provision:integration-test-db`
2. or confirm that CI verification is sufficient for this PR

## rewind instruction

if this handoff is rejected:
```
rhx route.stone.set --stone 5.3.verification --as rewound
```
