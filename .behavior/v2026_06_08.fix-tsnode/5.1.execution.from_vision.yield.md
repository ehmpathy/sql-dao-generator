# execution: remove ts-node from sql-dao-generator

## todos

- [x] remove `require('ts-node').register()` from bin/run
- [x] move ts-node from dependencies to devDependencies
- [x] run tests to verify (types ✓, format ✓, build ✓)

## done

### bin/run
```diff
-require('ts-node').register();
```

### package.json
```diff
-    "ts-node": "10.9.2",
```
(kept in devDependencies for local dev)

## note

types error about `@types/lodash.omit` is from npm→pnpm migration, unrelated to this fix.
