# blocker: 1.upgrade.invoke

## what blocks me

git.commit.uses is globally blocked. i cannot stage the changes from the upgrade without human permission.

```
🐢 bummer dude...

🐚 git.stage.add
   └─ error: globally blocked

a human has blocked all mechanic operations
```

## what i tried

1. ran `rhx declapract.upgrade exec` — ✅ completed successfully
2. documented completion in `1.upgrade.invoke.v1.i1.md` — ✅ done
3. attempted `rhx git.stage.add .` — ❌ blocked by global permission

the upgrade itself succeeded. all automatic fixes were applied. the only constraint is the permission to stage files.

## what i need

lift the global blocker:

```sh
git.commit.uses allow --global
```

or grant local permission:

```sh
git.commit.uses set --quant 5 --push allow
```

once permission is granted, i can stage the upgrade changes and proceed to stone 2.detect.hazards.
