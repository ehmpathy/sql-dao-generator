# blocker: 3.1.repair.test.defects

## what blocks me

the `.dpdmrc.yaml` file contains `node_modules` in the exclude array.

```yaml
exclude:
  - __test_assets__
  - node_modules  # forbidden per guide
```

the guide says:
> node_modules must never be excluded
> if git diff shows any forbidden change to .dpdmrc.yaml: this is a BLOCKER

## what i tried

1. ran all tests with `THOROUGH=true` — 144 pass, 0 fail
2. documented all 8 defects with what/how/why/fix
3. completed 5 self-reviews (r1-r5)
4. r5 (has-respected-dpdm-exclude) found the BLOCKER

the `.dpdmrc.yaml` file was created by the declapract upgrade, not by my defect repairs. i did not modify this file.

## what i need

guidance on how to proceed:

**option 1**: remove `node_modules` from `.dpdmrc.yaml` exclude array
- this fixes the BLOCKER
- but may cause dpdm to scan node_modules (slow, may surface cycles)

**option 2**: keep `node_modules` and document as infrastructure feedback
- proceed to reflection stone (3.2)
- let infrastructure team decide

**option 3**: human decides the correct exclusion pattern

which option should i take?
